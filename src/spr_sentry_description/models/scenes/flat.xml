<?xml version="1.0" encoding="utf-8"?>
<!--
  flat.xml —— 平地场景（默认）
  机器人本体在 ../sentry_robot.xml，本文件只负责"世界"（地面/灯光/天空盒）。
  场景通过 launch 参数 scene:=flat|rough|ramp|jump 切换，控制器/接口无需改动。
-->
<mujoco model="sentry_scene_flat">
  <include file="../sentry_robot.xml"/>

  <statistic center="0 0 0.2" extent="1.5"/>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.1 0.1 0.1" specular="0 0 0"/>
    <rgba haze="0.15 0.25 0.35 1"/>
    <global azimuth="120" elevation="-20"/>
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512" height="3072"/>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3"
      markrgb="0.8 0.8 0.8" width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5"/>
  </asset>

  <worldbody>
    <light pos="0 0 3" dir="0 0 -1" directional="true"/>
    <geom name="floor" type="plane" size="6 6 0.1" material="groundplane" rgba="0.6 0.6 0.6 1"/>
  </worldbody>
</mujoco>
